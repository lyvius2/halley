# CLAUDE.md

이 저장소의 작업 규칙은 **[AGENTS.md](./AGENTS.md)** 에 정의되어 있다.

작업을 시작하기 전에 반드시 `AGENTS.md`를 먼저 읽고, 그 내용(빌드·실행 방법, 고정 스택, 절대 규칙, 코딩 규칙, 하지 말 것)을 따를 것.

## 아키텍처 설계서

전체 아키텍처 설계는 **[docs/DESIGN.md](./docs/DESIGN.md)** 에 있다 (ERD, API 명세, 화면 정의, 결정 이력).

다음 경우에는 구현을 시작하기 전에 `docs/DESIGN.md`를 먼저 읽을 것:

- 이 프로젝트에서 처음 작업하는 경우(초기 개발 단계)
- memory에 이 프로젝트에 대한 정보가 없어 설계 맥락을 알 수 없는 경우
- 신규 API 엔드포인트, 외부 연동, 새 패키지·테이블을 추가하려는 경우

설계서에 해당 결정이 없으면 임의로 구현하지 말고 질문할 것.

## 우선순위

규칙이 상충할 경우 `docs/DESIGN.md` > `AGENTS.md` > 이 파일 순으로 따른다.